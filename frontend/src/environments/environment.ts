export const environment = {
  production: false,
  // .NET 8 backend (TahdcoUdp.Api). Leave empty ('') to run purely on the
  // bundled JSON (assets/data/dashboard-data.json) without any backend.
  apiUrl: 'https://onedashboard-v1.pixoustech.app'
  // apiUrl: 'http://localhost:5000'
};
